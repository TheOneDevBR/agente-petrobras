# LLM local — substitui anthropic SDK
requests>=2.31.0
beautifulsoup4>=4.12.0
ddgs>=9.0.0                 # busca DuckDuckGo (novo nome; importado como `from ddgs import DDGS`)
duckduckgo-search>=7.0.0   # fallback legado (import duckduckgo_search)
googlesearch-python>=1.3.0

# API REST (FastAPI) — backend do app web
fastapi>=0.110.0
uvicorn>=0.27.0

# Utilitários
python-dotenv>=1.0.0

# Renderização de páginas JavaScript/SPA (opcional; opt-in via AGENTE_RENDER_JS=1)
# Após instalar o pacote, baixe o navegador: playwright install chromium
playwright>=1.40.0

# RAG local sobre apostilas (opcional) — embeddings locais ONNX, sem API.
# Indexe com: python cli_python/rag.py --indexar <pasta>
chromadb>=0.5.0

# Dashboard web
streamlit>=1.40.0
plotly>=5.24.0

# Testes
pytest>=9.0.0
pytest-timeout>=2.4.0
pytest-xdist>=3.8.0
opendataloader-pdf  # extração estruturada de PDF (Java 11+); resolve mojibake e layout 2-colunas
pytest-cov>=6.0.0

